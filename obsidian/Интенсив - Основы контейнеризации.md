sudo unshare -m bash
%% Запустить bash, но с другим namespace %%



mount --bind astra18 astra18
%% Монтирование директории саму в себя
Появляется новое монтирование 
 %%

Чтобы поменять корневую файловую систему, старую корневую систему нужно переместить.

mkdir old_root
pivot_root . old_root
ls -l /
ls -l old_root

%% Видим файловую систему контейнера, то есть переключили %%

В контейнере мы не видим корневую файловую систему, но управлять процессами мы можем. То есть изоляция реализована только на файловой системе. 
1 аспект из 8


/var/tmp не удаляется при перезагрузке
/tmp удаляется

docker контейнер - это простой архив файловой системы.

Если стоит задача работы с контейнерами, то с контейнерами лучше заняться автоматизацией. 

astra ubi - универсальные образы astra.

